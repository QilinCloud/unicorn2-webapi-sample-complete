# Samples Legacy Landscape

The historic Unicorn ApiWeb sample mixed all methods in one example area.

This repository keeps the implementation isolated so it can be published as its own GitHub repository without shared files or hidden dependencies.

