# Complete ApiWeb Sample Architecture

Every exported function corresponds to one ApiWeb protocol method. Most write methods use `apiWebSuccessWithShopId`, while read methods add collection entries to the provided `Result`.

