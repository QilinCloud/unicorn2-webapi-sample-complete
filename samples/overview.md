# Samples Overview

This folder contains the marketplace-facing implementation variants for this standalone sample repository.

In this repository only `complete/` is active. It contains the full OTTO Markets style Web API connector implementation that is loaded by `classes/class.config.php` through the local `config.php` setting.

