# Domain Notes

OTTO product updates are not simple CRUD. A real production connector must track
asynchronous product tasks, quota limits, order state and permissions. This
sample returns clear ApiWeb errors when mapping or transport fails.
